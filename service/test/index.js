const assert = require("assert");
const csyn = require("../src/index");

describe("canonical-synonyms", ()=> {
    describe("#load", ()=> {
        it("should load from a json file", () => {
            
        });
    });
})